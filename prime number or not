#include <stdio.h>

int main() {
    int n,i,c=0;
    scanf("%d",&n);
    for(i=2;i<n;i++)
    {
        if(n%i==0)
        {
            c=i;
            break;
        }
    }
    if(c==0&&n!=1)
    printf("prime no");
    else
    printf("not a prime no");
    return 0;
}
